<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ConcertRepository">

    <select id="getConcerts" resultType="com.example.demo.vo.Concert">
        SELECT *, IF(reviewCount > 0, ROUND(totalRating / reviewCount, 1), 0) AS extra__avgRating
        FROM concert
        ORDER BY startDate ASC
    </select>

    <select id="getConcertById" resultType="com.example.demo.vo.Concert">
        SELECT *, IF(reviewCount > 0, ROUND(totalRating / reviewCount, 1), 0) AS extra__avgRating
        FROM concert
        WHERE id = #{id}
    </select>

    <insert id="insertConcert" parameterType="com.example.demo.vo.Concert"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO concert
        (title, posterImg, startDate, endDate, bookingStartAt, body, status, regDate, updateDate)
        VALUES
        (#{title}, #{posterImg}, #{startDate}, #{endDate}, #{bookingStartAt},
         #{body}, IFNULL(#{status}, 'DRAFT'), NOW(), NOW())
    </insert>

    <update id="updateConcert" parameterType="com.example.demo.vo.Concert">
        UPDATE concert
        SET title         = #{title},
            <if test="posterImg != null">posterImg = #{posterImg},</if>
            startDate     = #{startDate},
            endDate       = #{endDate},
            bookingStartAt= #{bookingStartAt},
            body          = #{body},
            updateDate    = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateConcertStatus" parameterType="map">
        UPDATE concert
        SET `status` = #{status}, updateDate = NOW()
        WHERE id = #{id}
    </update>

    <select id="getSalesStatsByConcertId" resultType="map">
        SELECT IFNULL(SUM(s.totalSeats), 0)   AS totalSeats,
               IFNULL(COUNT(r.id), 0)          AS reservedCount,
               IFNULL(SUM(r.paidPrice), 0)     AS revenue
        FROM `schedule` s
        LEFT JOIN reservation r ON s.id = r.scheduleId AND r.status = 'CONFIRMED'
        WHERE s.concertId = #{concertId}
    </select>

    <update id="incrementViewCount">
        UPDATE concert SET viewCount = viewCount + 1 WHERE id = #{id}
    </update>

    <insert id="insertArtist" parameterType="com.example.demo.vo.Artist"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO artist (`name`, profileImg)
        VALUES (#{name}, #{profileImg})
    </insert>

</mapper>
