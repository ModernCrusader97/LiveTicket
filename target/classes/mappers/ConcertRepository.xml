<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ConcertRepository">

	<select id="getConcerts"
		resultType="com.example.demo.vo.Concert">
		SELECT * FROM concert
		ORDER BY startAt ASC
	</select>

	<select id="getConcertById"
		resultType="com.example.demo.vo.Concert">
		SELECT * FROM concert
		WHERE id = #{id}
	</select>

	<select id="getRemainingSeats" resultType="map">
		SELECT SG.name AS gradeName,
		SG.price,
		COUNT(S.id) AS remainCount
		FROM seatGrade AS SG
		LEFT JOIN seat AS S
		ON SG.id = S.gradeId
		AND S.`status` = 'AVAILABLE'
		WHERE SG.concertId = #{concertId}
		GROUP BY SG.id
	</select>

</mapper>